<UserControl x:Class="Xc.HiKVisionSdk.Isc.Wpf.Pages.Resource.PersonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Xc.HiKVisionSdk.Isc.Wpf.Pages.Resource"
             mc:Ignorable="d" 
             xmlns:s="https://github.com/canton7/Stylet"
             d:DataContext="{d:DesignInstance local:PersonViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal" >
        <!--<Button Command="{s:Action PersonList}" Margin="10,5">获取人员列表V2</Button>-->
        <Button Command="{s:Action OrgList}" Margin="10,5">获取组织列表</Button>
        <Button Command="{s:Action PersonList}" Margin="10,5">获取人员列表</Button>
        <Button Command="{s:Action CardList}" Margin="10,5">卡片信息</Button>
    </StackPanel>
</UserControl>
